export const connection: Connection = {
  CONNECTIONS_STRING: 'CONNECTION_STRING',
  DB: 'MYSQL',
  DBNAME: 'TEST_NEST_JS',
};
export type Connection = {
  CONNECTIONS_STRING: string;
  DB: string;
  DBNAME: string;
};
